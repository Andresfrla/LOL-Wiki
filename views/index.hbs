{{!-- <h1>{{appTitle}} </h1>
{{#each names}}
    <h2>{{name}}</h2>
{{/each}}

{{#each urls}}
  <img src={{url}} alt="" class="m-4">
{{/each}}
  --}}

{{!-- <div class="row">
  {{#each urls}}
    <div class="col-md-3"> <!-- Reducido el tamaño de la columna a col-md-3 -->
      <div class="card mb-4">
        <img src={{url}} alt="" class="card-img-top img-fluid"> <!-- Agregada la clase img-fluid para que la imagen sea más flexible -->
        <div class="card-body">
          <h5>{{name}}</h5>
          <h5 class="card-title">Título de la imagen</h5>
          <p class="card-text">Descripción de la imagen.</p>
        </div>
      </div>
    </div>
  {{/each}}
</div> --}}

<div class="row">
  {{#each champions}}
    <div class="col-md-3"> <!-- Reducido el tamaño de la columna a col-md-3 -->
      <div class="card mb-4">
        <a href="/champions/detail/{{name}}">
          <img src="{{url}}" alt="" class="card-img-top img-fluid">
        </a> 
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5> <!-- Reemplazado "Título de la imagen" por {{name}} -->
          <p class="card-text">{{description}}</p>
        </div>
      </div>
    </div>
  {{/each}}
</div>


